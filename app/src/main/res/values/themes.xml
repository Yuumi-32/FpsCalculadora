<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FpsCalculadora" parent="android:Theme.Material.NoActionBar">
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>
    </style>

    <!-- Tela de abertura via core-splashscreen: mesmo fundo escuro do app, o
         ícone do launcher, e volta pro tema normal assim que a Activity chama
         installSplashScreen(). -->
    <style name="Theme.FpsCalculadora.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.FpsCalculadora</item>
    </style>
</resources>
