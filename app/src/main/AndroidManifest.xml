<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Usada pelo catálogo remoto: o app busca a base de hardware, jogos e
      preços publicada em https://yuumi-32.github.io/FpsCalculadora/, para os
      dados não envelhecerem junto com o APK instalado.

      Só o código Kotlin faz esse pedido. A WebView (que sobrou apenas no
      build de debug) continua trancada nos assets locais — ver
      shouldInterceptRequest na MainActivity.

      Nada do usuário sai do aparelho: é um GET de arquivo estático público,
      sem corpo, sem query e sem identificador.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Evita a tentativa de download quando o aparelho está sabidamente offline. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FpsCalculadora.Splash"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="false">

        <!--
          A UI nativa em Compose é o app publicado desde a Fase 2. A WebView
          que ocupava este lugar continua no repositório e no build de debug,
          como referência de comparação — ver src/debug/AndroidManifest.xml.
        -->
        <activity
            android:name=".ComposeMainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
