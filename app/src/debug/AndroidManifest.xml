<?xml version="1.0" encoding="utf-8"?>
<!--
  Só na variante debug: expõe a UI Compose da Fase 2 como um segundo ícone,
  ao lado do app WebView, para comparar as duas telas no mesmo aparelho.
  O release continua abrindo apenas a MainActivity.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".ComposeMainActivity"
            android:exported="true"
            android:label="FPS Compose"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
