<?xml version="1.0" encoding="utf-8"?>
<!--
  Só na variante debug: mantém a UI WebView acessível como um segundo ícone,
  ao lado do app Compose, para comparar a tela publicada com a original no
  mesmo aparelho.

  A relação era a inversa até a Fase 2 terminar — a WebView era o release e a
  Compose o ícone extra. O release agora abre apenas a ComposeMainActivity, e
  esta Activity não existe no manifesto de produção.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="FPS WebView (antigo)"
            android:screenOrientation="portrait"
            android:configChanges="screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
